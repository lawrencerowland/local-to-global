# Scope essay consolidation — 6 September 2026

Lawrence requested: enhance 001 and retire 005. Essay 001 carries the shared Pennine Viaduct demonstration; 005 is retired as an essay. Its former URL contains only a redirect and fallback link. Original content remains in Git history.

## Capability impact

| Existing capability | Current disposition |
|---|---|
| Four work-package registers, expand/collapse and conflict detection | Retained in 001; keyboard controls and counts derived from interface results added. Uncompared scope rows stay neutral. |
| Graph of six interfaces, selectable packages and interfaces | Retained in 001; graph follows the selected state and the two diagonal labels have separate targets. Text controls provide an equivalent route. |
| Local sections, restriction maps and gluing explanation | Extended with a concrete field projection and explicit distinction between finite compatibility rules and a full sheaf construction. |
| Interface checker | Extended with visible input pairs, rule descriptions and original / drainage-agreed / fully-reconciled states. |
| Resolution walkthrough and reset | Corrected: agreeing drainage ownership changes I1 and leaves remaining issues open. Reset restores the original values. |
| PMO value and validation sequence | Retained and extended with scope selection, data contract, review cadence, decision authority, evidence and empirical evaluation. Unsupported cost totals and implied measured benefits removed. |
| 005 as a separate essay | Lawrence-approved retirement. Comparison found no unique demonstration or interaction; active capabilities are in 001. |
| Essays 002, 003, 004, 006, 007, 008 | Retained; shared navigation version updated only. |

## Interface expectations

| State | Pass | Conflict | Missing information |
|---|---:|---:|---:|
| Original accounts | 2 | 2 | 2 |
| Drainage ownership agreed | 3 | 1 | 2 |
| All six interfaces reconciled | 6 | 0 | 0 |

The figures count interfaces, not affected scope-register rows. One conflict can affect two registers. A passing result applies only to the six declared rules; the page does not establish project completeness, engineering adequacy, approval or savings.

## User action closure and evidence

The controls deliberately change temporary teaching state. The page explains this before interaction and repeats it alongside the result. Choosing a state yields an accessible named result with changed facts and residual issues. The same state remains visible across sections; changing the selection or using Reset corrects it; reloading restores original data while retaining the selected section URL.

Saving, durable receipts, Files, History, attribution to a real approver and withdrawal of a real decision are not applicable: no record or file is created and there is no backend write. The current selection's on-page result is a temporary explanation, not a saved decision. Original examples and URLs remain reproducible on the public site.

Fresh-browser verification covers the detector, all three state totals, projected fields and missing-data cases, both diagonal graph targets, drainage agreement and whole-slice recheck, cross-section consistency, reset, reopen/reload, keyboard package selection, desktop and 390px layout, seven active essay links, 005 redirection, and runtime errors. These are agent-operated browser checks, not human-user acceptance or field validation.
